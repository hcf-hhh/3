<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aed.aedend.mapper.UserMapper">

    <select id="queryByName" resultType="com.aed.aedend.entity.User">
        select *
        from user
        where name = #{name}
    </select>

    <select id="findById" resultType="com.aed.aedend.entity.User">
        select *
        from user
        where id = #{id}
    </select>

    <update id="delete">
        update user
        set is_deleted = 1
        where id in
        <foreach collection="ids" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>
    <select id="findAll" resultType="com.aed.aedend.entity.User">
        select *
        from user
        where is_deleted = 0
    </select>
    <select id="findByName" resultType="com.aed.aedend.entity.User">
        select *
        from user
        where name = #{name}
    </select>
</mapper>